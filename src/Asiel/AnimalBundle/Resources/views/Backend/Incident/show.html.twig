<div class="ui segment">
    <h3 class="ui header">Incident {{ incident.id }}</h3>
    <p>Datum: {{ incident.date|date('d-m-Y') }}</p>
    <p>Omschrijving: {{ incident.title }}</p>
    <p>Gebeurtenis: {{ incident.description|raw }}</p>
</div>