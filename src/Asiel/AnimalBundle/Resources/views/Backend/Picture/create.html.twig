{% extends '@Backend/Base/Base.html.twig' %}
{% block pagesubtitle %}Foto Uploaden{% endblock %}
{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-sm-4">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                {{ outputFormButton('Uploaden') }}
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}