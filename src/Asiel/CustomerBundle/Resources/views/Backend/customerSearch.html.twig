<div class="container">
    <div class="row">
        <div class="col-xs-10">
            <table id="resulttable" class="ui table">
                <thead>
                <tr>
                    <th>Voornaam</th>
                    <th>Achternaam</th>
                    <th>Geboortedatum</th>
                    <th>Adres</th>
                    <th>Zwartelijst</th>
                </tr>
                </thead>
                <tbody>
                {% for customer in results %}
                    <tr id="{{ customer.id }}"
                            {% if customer.blacklisted %}
                                class="customer negative"
                            {% else %}
                                class="customer"
                            {% endif %}
                    >
                        <td class="firstname">{{ customer.firstname }}</td>
                        <td class="lastname">{{ customer.lastname }}</td>
                        <td>{{ customer.dayofbirth|date('d-m-Y') }}</td>
                        <td>{{ customer.address }} {{ customer.housenumber }}</td>
                        <td>
                            {% if customer.blacklisted %}
                                {% if customer.blacklistedReason|length == 0 %}
                                    Geen reden beschikbaar
                                {% endif %}
                                {{ customer.blacklistedReason|slice(0,50) }}
                            {% else %}
                                {{ outputBool(customer.blacklisted) }}
                            {% endif %}
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>